import ToolContent from "./ToolContent";
export const metadata = { title: "Compare TikTok Accounts | SIGNAL", description: "Compare two TikTok creators side by side — engagement, growth, audience quality, and content analyzed by AI." };
export default function Page() { return <ToolContent />; }
